<h1> Look at all this crap that we need to remember</h1>
	<ul>

	<% @lists.each do |list|  %>
	<li>
		<%= link_to list.title, list_path(list) %>
		<% end %>
	</li>
	</ul>
	<%= link_to "Create a new list",  new_list_path %>